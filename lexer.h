// Importing libraries
#include "token.h"

class Lexer {
    private:
        // Declaring atributes
        string input;                       // Store the input text
        int pos;                            // Store the position in the input text
    
    public:
        // Builder
        Lexer(string);
};